version: 0.4
name: memtier_benchmark-10streams-xreadgroup-count-100
description: >
  Multi-stream XREADGROUP benchmark with 10 streams. Pre-loads 10 streams with
  ~1M entries each (10M seed), creates a consumer group on each. Benchmark runs
  70% XADD (producing) + 30% XREADGROUP COUNT 100 (consuming) to random streams,
  ensuring entries are always available. Tests radix-tree lookup at low stream-count
  baseline.
dbconfig:
  configuration-parameters:
    save: '""'
  check:
    keyspacelen: 10
  preload_tool:
    run_image: redislabs/memtier_benchmark:edge
    tool: memtier_benchmark
    arguments: >
      "--data-size" "100"
      --command "XADD stream:__key__ * field __data__" --command-key-pattern P
      --key-minimum 1 --key-maximum 10 --key-prefix ""
      -n 50000 -c 50 -t 4 --hide-histogram
  init_commands:
    - EVAL "for i=1,10 do redis.call('XGROUP','CREATE','stream:'..i,'test-group','0','MKSTREAM') end return 'OK'" 0
  resources:
    requests:
      memory: 4g
  dataset_name: 10streams-1M-entries-each-with-consumer-group
  dataset_description: 10 stream keys with ~1M entries each (10M seed), each with a consumer group named 'test-group'.
tested-commands:
- xadd
- xreadgroup
tested-groups:
- stream
redis-topologies:
- oss-standalone
build-variants:
- gcc:15.2.0-amd64-debian-bookworm-default
- gcc:15.2.0-arm64-debian-bookworm-default
- dockerhub
clientconfig:
  run_image: redislabs/memtier_benchmark:edge
  tool: memtier_benchmark
  arguments: >
    --data-size 100
    --command "XADD stream:__key__ * field __data__" --command-key-pattern R --key-minimum 1 --key-maximum 10 --key-prefix "" --command-ratio 70
    --command "XREADGROUP GROUP test-group consumer1 COUNT 100 STREAMS stream:__key__ >" --command-key-pattern R --key-minimum 1 --key-maximum 10 --key-prefix "" --command-ratio 30
    --hide-histogram
    --test-time 120 -c 25 -t 4
  resources:
    requests:
      cpus: '4'
      memory: 2g
priority: 94
