version: 0.4
name: memtier_benchmark-Nclients-xread-block-consumer-group
description: >
  Blocking stream consumer-group unblock-latency sentinel (see issue #402).
  Exercises the blocked-to-unblocked client code path in blocked.c
  (blockForKeys with the stream/consumer-group path, handleClientsBlockedOnKeys,
  serveClientBlockedOnKey -> streamReplyWithRange for the group, streamID PEL
  bookkeeping, processUnblockedClients, unblockClient) which no existing
  memtier_benchmark spec covers. One producer (a single connection) issues XADD
  onto a single stream (bounded with MAXLEN ~ 10000 so memory stays flat) while
  N=100 consumer connections (-c 25 -t 4) each run
  XREADGROUP GROUP g c1 BLOCK 0 COUNT 1 STREAMS stream:1 > with pipeline 1.
  Because the consumers vastly outnumber the single producer, there is rarely a
  backlog of new entries, so almost every XREADGROUP registers as blocked-on-key
  and is later served by an XADD -- driving traffic through the stream unblock
  path on essentially every op and growing the processUnblockedClients queue that
  beforeSleep() drains each tick. The exported Totals are the consumer group's
  (last clientconfig): Ops/sec is the unblock rate and p50/p99 are the unblock
  latency. MULTI-CLIENT SUITE: runs only on a runner with
  BENCHMARK_MULTITOOL_ENABLED=1 (the coordinator skips it otherwise).
  EXPORT-PROVENANCE INVARIANT: the multi-client runner exports the LAST memtier
  clientconfig's JSON, so the XREADGROUP consumer group MUST stay last. CLEAN
  SHUTDOWN: the producer uses a slightly longer --test-time than the consumers
  (125 vs 120) purely so it outlives them -- any consumer still parked in a
  blocking XREADGROUP when its own test-time expires gets one final XADD delivered
  and returns, instead of hanging forever on BLOCK 0. The producer does NOT "drain
  the backlog": once the consumers stop, the small ~5s producer tail simply XADDs
  new entries that nobody reads (they age out of the MAXLEN ~ 10000 window and pile
  up as unread group lag, capped by MAXLEN). Do NOT run this suite with a single
  uniform --override-memtier-test-time (it would stop the producer at the same
  instant and could leave a blocked consumer hanging). MEMORY CEILING: the stream
  is capped with MAXLEN ~ 10000 so its size stays flat regardless of the run
  length; the ~5s solo-producer tail only lets group lag climb toward that same
  ~10000 ceiling, so there is no memory balloon here (unlike the BLPOP/BZPOPMIN
  suites, whose keys are uncapped). VERIFYING THE BLOCKING PATH: there is no
  in-spec assertion for this (the runner's dbconfig.check only asserts keyspacelen
  at benchmark start, not blocked_clients during the run). To confirm the workload
  is genuinely blocking, poll `INFO clients` and the consumer-group lag
  (`XINFO GROUPS stream:1`) every 1s while the suite runs: on the blocking path
  blocked_clients approaches the consumer connection count (~100) and the group
  lag stays ~0. NOTE the correct signal here is group lag, not "key empty": XLEN
  sits at the MAXLEN cap (~10000) throughout because stream reads never delete
  entries, so an empty stream is NOT expected. If blocked_clients ~0 and group lag
  grows, the workload has regressed to the non-blocking fast path. Measured locally
  with a reduced -c 25 -t 2 (= 50 conns) copy: blocked_clients held at 43-49, XLEN
  pinned at the ~10000 MAXLEN cap and group lag 0 for the entire measured window
  (confirmed on the blocking path).
dbconfig:
  configuration-parameters:
    save: '""'
  check:
    keyspacelen: 1
  init_commands:
  - XGROUP CREATE stream:1 test-group $ MKSTREAM
  dataset_name: 1stream-empty-with-consumer-group
  dataset_description: A single empty stream (created via MKSTREAM) with a consumer group named 'test-group' reading only new entries.
  resources:
    requests:
      memory: 1g
# tested-commands lists every verb the workload sends (both clientconfigs): xadd
# is the producer feed; xreadgroup is the measured/exported blocking consumer command.
tested-commands:
- xadd
- xreadgroup
tested-groups:
- stream
redis-topologies:
- oss-standalone
build-variants:
- gcc:15.2.0-amd64-debian-bookworm-default
- gcc:15.2.0-arm64-debian-bookworm-default
- dockerhub
clientconfigs:
# Producer (index 0): 1 connection appending rotating 32B entries to the single
# stream, capped with MAXLEN ~ 10000 so the stream does not grow unbounded over
# the run. --test-time 125 is a small tail past the consumers' 120 so the producer
# outlives them (the last blocked XREADGROUP gets one final entry and returns); it
# is NOT draining a backlog. The MAXLEN cap keeps memory flat regardless of tail
# length.
- run_image: redislabs/memtier_benchmark:edge
  tool: memtier_benchmark
  arguments: >-
    --data-size 32
    --command "XADD stream:1 MAXLEN ~ 10000 * field __data__"
    -c 1 -t 1 --pipeline 1 --distinct-client-seed
    --test-time 125 --hide-histogram
  resources:
    requests:
      cpus: '1'
      memory: 1g
# Consumer group (index 1, LAST => exported): N=100 connections each blocking on
# the single stream with XREADGROUP ... BLOCK 0 reading only new entries (>) and
# pipeline 1. All connections share consumer name c1; Redis delivers each new
# entry to one blocked consumer. Its Totals p50/p99 are the unblock latency and
# Ops/sec the unblock rate.
- run_image: redislabs/memtier_benchmark:edge
  tool: memtier_benchmark
  arguments: >-
    --command "XREADGROUP GROUP test-group c1 COUNT 1 BLOCK 0 STREAMS stream:1 >"
    -c 25 -t 4 --pipeline 1
    --test-time 120 --print-percentiles 50,99 --hide-histogram
  resources:
    requests:
      cpus: '4'
      memory: 2g
exporter:
  redistimeseries:
    timemetric: $."ALL STATS".Runtime."Start time"
    metrics:
    - $."ALL STATS".Totals."Ops/sec"
    - $."ALL STATS".Totals."Latency"
    - $."ALL STATS".Totals."Percentile Latencies"."p50.00"
    - $."ALL STATS".Totals."Percentile Latencies"."p99.00"
priority: 50
