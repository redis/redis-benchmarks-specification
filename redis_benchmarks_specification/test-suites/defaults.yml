
exporter:
  redistimeseries:
    break_by:
    - version
    - commit
    timemetric: $."ALL STATS".Runtime."Start time"
    metrics:
    - $."*".Totals."Ops/sec"
    - $."*".Totals."Latency"
    - $."*".Totals."Misses/sec"
    - $."*".Totals."Percentile Latencies"."p50.00"
